{
  "// NOTE": "Configuration examples for Claude Code with Dockerized MailSandbox",
  "// USAGE": "Use the CLI commands below rather than JSON configuration",
  
  "// RECOMMENDED": "Use Claude Code CLI instead:",
  "// claude mcp add mailsandbox-docker -- docker exec -i mailsandbox mailsandbox --mcp-server --mcp-transport stdio",
  
  "mcpServers": {
    "mailsandbox-docker-stdio": {
      "// DESCRIPTION": "Connect to Dockerized MailSandbox via stdio",
      "command": "docker",
      "args": [
        "exec", "-i", "mailsandbox-mcp",
        "mailsandbox", "--mcp-server", "--mcp-transport", "stdio",
        "--database", "/data/mailsandbox.db"
      ],
      "// NOTE": "Requires: docker run with name 'mailsandbox-mcp'"
    },
    
    "mailsandbox-docker-exec": {
      "// DESCRIPTION": "Execute MailSandbox commands in Docker container",
      "command": "docker",
      "args": [
        "exec", "-i", "mailsandbox-mcp",
        "mailsandbox", 
        "--mcp-server",
        "--mcp-transport", "stdio",
        "--database", "/data/mailsandbox.db"
      ],
      "// NOTE": "Requires: docker container named 'mailsandbox-mcp' to be running"
    },

    "mailsandbox-compose-exec": {
      "// DESCRIPTION": "Execute via docker-compose (recommended for development)",
      "command": "docker-compose", 
      "args": [
        "-f", "examples/docker-compose.mcp.yml",
        "exec", "-T", "mailsandbox",
        "mailsandbox",
        "--mcp-server", 
        "--mcp-transport", "stdio",
        "--database", "/data/mailsandbox.db"
      ],
      "// NOTE": "Run from project root directory"
    },
    
    "mailsandbox-local-with-docker-data": {
      "// DESCRIPTION": "Local MailSandbox binary accessing Docker volume data",
      "command": "mailsandbox",
      "args": [
        "--mcp-server",
        "--mcp-transport", "stdio",
        "--database", "/var/lib/docker/volumes/mailsandbox_mailsandbox-data/_data/mailsandbox.db"
      ],
      "// NOTE": "Adjust volume path based on your Docker setup"
    }
  }
}