{
  "mcpServers": {
    "mailsandbox": {
      "command": "mailsandbox",
      "args": [
        "--mcp-server",
        "--mcp-transport", "stdio", 
        "--database", "${workspaceFolder}/.mailsandbox/mailsandbox.db",
        "--listen", "127.0.0.1:8025",
        "--smtp", "127.0.0.1:1025"
      ],
      "env": {
        "MP_MCP_SERVER": "true",
        "MP_MCP_TRANSPORT": "stdio",
        "MP_DATA_FILE": "${workspaceFolder}/.mailsandbox/mailsandbox.db"
      }
    },
    "mailsandbox-with-postmark": {
      "command": "mailsandbox", 
      "args": [
        "--mcp-server",
        "--mcp-transport", "stdio",
        "--postmark-api",
        "--postmark-token", "dev-token-123", 
        "--database", "${workspaceFolder}/.mailsandbox/mailsandbox.db"
      ],
      "env": {
        "MP_MCP_SERVER": "true",
        "MP_POSTMARK_API": "true",
        "MP_POSTMARK_TOKEN": "dev-token-123",
        "MP_DATA_FILE": "${workspaceFolder}/.mailsandbox/mailsandbox.db"
      }
    }
  }
}